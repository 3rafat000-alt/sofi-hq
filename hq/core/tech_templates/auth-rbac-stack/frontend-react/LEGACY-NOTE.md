# ⚠️ LEGACY — retired by owner decision R2 (2026-08-23 · SOFI-HQ-INT-0003)

> **This folder (the React interface) is frozen from development** — it remains a maintenance reference for existing projects only.
> Every new web project is built in **Flutter Web** per `hq/core/standards/stacks-tech.md §Decision R2` on the same design system and the same OpenAPI contract consumed by mobile.
>
> The Flutter Web counterpart of this template is derived at the birth of the first new project via `sofi-project-spawn` — no manual copies outside the v2 line.
