# Article 08 — Handoff Law (tickets, boundaries, sign-off)

Foundation: serves Teaching II (Hierarchical Flow) and Teaching V (Continuous Metamorphosis). Read `hq/core/constitution-master.md` first.

## The ticket

*_(Example updated per INT-GTW-024 · 2026-08-24 — the old simplified TKT-NNN schema was retired 2026-07-17 by owner order; the canonical form below comes from `hq/core/nexus/bus/ticket-schema.md` and is generated by the sofi-handoff skill):_*

```md
## TKT-PRJ-0001 · RCCF handoff
rccf_ref: WO-2026-08-24-001
from_agent: arc-lead
to_agent:   brd-ceo          # upward only — cross-room passes through both Leads (P-02.7)
objective:  model entities for audit-log; reversible migrations.
success_metric: schema-contract approved by dat-lead + rollback rehearsed
artifacts:  [schema-contract.md, ERD.md, migrations-plan.md]
evidence:   <full sofi-evidence block — file:line for every change and exit code for every command>
status:     ready-for-review
handoff_note: threat-model attached within the package
```

Lifecycle: `open → accepted → ready-for-review → done | rejected | blocked` (sole source: `hq/core/nexus/bus/ticket-schema.md`). The room Lead operates the queue (dispatch tooling retired 2026-07-16 → hierarchical enforcement).

## Room-boundary validation

Ticket `from:`/`to:` same room, agent→Lead, Lead→Lead, or boardroom/gateway→Lead. Specialist never addresses another room's specialist directly. Violation fails the gate.

## Verbatim forwarding

Lead forwards findings verbatim, citations and evidence intact. Never re-author.

## Gate sign-off

- Producer: mark `done` only with evidence block.
- Receiver: `accepted` after fresh-context check against original criteria.
- Gate advance: gtw-gatekeeper adversarial verdict.

## Claims & LOCKS

Path claims are recorded by the claiming agent in `projects/<PRJ>/brain/HANDOFFS.md` (path-glob + agent-id + ticket); release removes the entry. The room Lead approves claim and release (claim tooling retired 2026-07-16 → hierarchical enforcement).

## Session continuity

Every handoff records the producer's latest checkpoint in HIPPOCAMPUS + project `brain/CONTEXT.md`. Next session resumes exactly.
