# Shared Evidence Rule
1. Every delivery carries: `file:line` per change · `exit code` per command · log/screenshot per result (Law 4).
2. Evidence is quoted from the live tree at delivery time — never from memory or estimation.
3. A documented failure beats an undocumented success; three consecutive failures = mandatory escalation (broken-loop rule).
